# Copy to .env and fill in. Never commit .env.
# Key needs the "ElevenAgents: Write" permission only.
ELEVENLABS_API_KEY=sk_your_key_here

# Optional: reuse existing resources instead of creating duplicates
KB_DOC_ID=
AGENT_ID=
